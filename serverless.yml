service: qiita-lgtm-ranking

frameworkVersion: '2'

provider:
  name: aws
  runtime: go1.x

package:
  exclude:
    - ./**
  include:
    - ./build/**

functions:
  updateWeekly:
    handler: build/updateWeekly
